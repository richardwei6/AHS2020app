<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- TODO Remove the following "exclude" elements to make them a part of the auto backup -->
    <exclude
        domain="database"
        path="NotifDatabase" />
    <exclude
        domain="database"
        path="saved_table" />
    <exclude
        domain="database"
        path="current_table" />
    <exclude
        domain="database"
        path="BulletinDatabase" />
    <!-- Exclude the shared preferences file that contains the GCM registrationId -->
    <exclude
        domain="sharedpref"
        path="font_setting.xml" />
    <exclude
        domain="sharedpref"
        path="NOTIF_ID_FILE_KEY.xml" />
    <exclude
        domain="sharedpref"
        path="notif settings.xml" />
</full-backup-content>
